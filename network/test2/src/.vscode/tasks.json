    {
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "-std=c++11",
                "-I${workspaceFolder}/../../../include/",
                //"${file}",
                "${fileDirname}/**.cpp",
                "-o",
                //"${fileDirname}/${fileBasenameNoExtension}"
                "${fileDirname}/test2"

            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}