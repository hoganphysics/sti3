    {
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "-std=c++11",
                "-I${workspaceFolder}/../../../include/",
                "-I${workspaceFolder}/",
                "-I/usr/include/",
                //"${file}",
                "${fileDirname}/**.cpp",
                "-shared",
                "-fPIC",
                "-o",
                //"${fileDirname}/${fileBasenameNoExtension}"
                "${fileDirname}/libstidevice.so"

            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}