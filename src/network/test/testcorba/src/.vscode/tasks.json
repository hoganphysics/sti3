    {
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "-std=c++11",
                "-std=c++14",
                "-pthread",
                "-I${workspaceFolder}/../../../../../include/",
                "-I${workspaceFolder}/../../../src/",
                "-I${workspaceFolder}/../../../../device/src/",
                //"-I/usr/include/",
                //"${file}",
                "${fileDirname}/**.cpp",
                //"-shared",
                //"-fPIC",
                "-o",
                //"${fileDirname}/${fileBasenameNoExtension}"
                "${fileDirname}/testcorba",
                "-L${workspaceFolder}/../../../src",
                "-L${workspaceFolder}/../../../../device/src",
                "-Wl,-rpath,.",     //for including a specific directory in the library search path (during run); here "."
                //"-Wl,-rpath,/usr/local/lib",
                "-lstidevice",
                "-lstinetwork"

            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}