# test-specific rules

## File sturcture

- Tests for the device library go in `test/device/`
- Tests for the network library go in `test/netork/`
- Each cpp file we test from `src/` gets its own test cpp file.
