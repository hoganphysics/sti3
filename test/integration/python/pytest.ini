[pytest]
testpaths = .
python_files = test_*.py
addopts = -ra
markers =
    integration: automated STI network integration tests
    slow: longer-running integration tests
    stress: scale, repetition, and load tests
    observe: opt-in tests that keep a network alive for frontend observation
    requires_nameservice: tests that require an omniORB name service
